/**
 * @file
 * drupal_sdc behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupalSdc = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
